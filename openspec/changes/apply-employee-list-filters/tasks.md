## 1. Implementation

- [ ] 1.1 Update employee filter hook APIs as needed to support explicit apply/remove interactions for active chips.
- [ ] 1.2 Replace the employee filter UI with `ListFilters`, including desktop popovers, mobile drawer, and active-filter chips.

## 2. Verification

- [ ] 2.1 Add focused coverage for employee filter apply/reset behavior if runtime orchestration changes.
- [ ] 2.2 Run targeted tests and repository checks relevant to the employee filter change.

