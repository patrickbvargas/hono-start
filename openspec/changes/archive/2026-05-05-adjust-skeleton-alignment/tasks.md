## 1. Shared Skeleton Alignment

- [x] 1.1 Update the shared list-route skeleton to remove border strokes and align placeholder heights with the common final list layout.
- [x] 1.2 Update the shared entity-detail skeleton fields to match final field heights and spacing without changing placeholder widths.

## 2. Verification

- [x] 2.1 Adjust focused skeleton contract tests to reflect the new shared structure.
- [x] 2.2 Run `pnpm check` and `npx tsc --noEmit`, then resolve any issues before closing the change.
