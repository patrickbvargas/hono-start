## 1. Implementation

- [x] 1.1 Update employee filter hook APIs as needed to support explicit apply/remove interactions for active chips.
- [x] 1.2 Replace the employee filter UI with `ListFilters`, including desktop popovers, mobile drawer, and active-filter chips.

## 2. Verification

- [x] 2.1 Add focused coverage for employee filter apply/reset behavior if runtime orchestration changes.
- [x] 2.2 Run targeted tests and repository checks relevant to the employee filter change.
