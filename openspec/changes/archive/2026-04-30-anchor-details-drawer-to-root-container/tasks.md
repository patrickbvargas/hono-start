## 1. Artifact And Implementation

- [x] 1.1 Anchor the authenticated root layout container so shared detail drawers can target it.
- [x] 1.2 Update the shared detail drawer wrapper and drawer UI primitive to render relative to the root container instead of the viewport.

## 2. Validation

- [x] 2.1 Run `pnpm check` and `npx tsc --noEmit`, then resolve any issues before closing the change.
