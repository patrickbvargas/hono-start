## 1. Implementation

- [x] 1.1 Update fee filter hook APIs as needed to support explicit apply/remove interactions for active chips.
- [x] 1.2 Replace the fee filter UI with `ListFilters`, preserving filter semantics and responsive behavior.

## 2. Verification

- [x] 2.1 Add focused coverage for fee filter apply/reset behavior if runtime orchestration changes.
- [x] 2.2 Run targeted tests and repository checks relevant to the fee filter change.
