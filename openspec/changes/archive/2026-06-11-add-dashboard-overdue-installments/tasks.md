## 1. OpenSpec And Data Contract

- [x] 1.1 Finalize dashboard spec delta and dashboard summary schema for overdue installments
- [x] 1.2 Extend dashboard data queries with due-installment detection and role-scoped filtering

## 2. Dashboard UI

- [x] 2.1 Add dashboard overdue-installments analytical component using shared UI/table patterns
- [x] 2.2 Compose the new table into the dashboard layout with pt-BR empty-state copy

## 3. Verification

- [x] 3.1 Add focused Vitest coverage for overdue-installment calculation, filter scoping, and empty state behavior
- [x] 3.2 Run `pnpm check` and `npx tsc --noEmit`, then fix any reported issues
