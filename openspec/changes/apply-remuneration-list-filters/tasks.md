## 1. Implementation

- [ ] 1.1 Update remuneration filter hook APIs as needed to support explicit apply/remove interactions for active chips.
- [ ] 1.2 Replace the remuneration filter UI with `ListFilters`, preserving admin-only filter behavior and responsive surfaces.

## 2. Verification

- [ ] 2.1 Add focused coverage for remuneration filter apply/reset behavior if runtime orchestration changes.
- [ ] 2.2 Run targeted tests and repository checks relevant to the remuneration filter change.

