## 1. Implementation

- [ ] 1.1 Update dashboard filter hook APIs as needed to support active-chip interactions for advanced filters only.
- [ ] 1.2 Replace the dashboard advanced filter UI with `ListFilters` while preserving inline period shortcuts and the inline admin employee filter.

## 2. Verification

- [ ] 2.1 Add focused coverage for dashboard filter orchestration if runtime behavior changes.
- [ ] 2.2 Run targeted tests and repository checks relevant to the dashboard filter change.

