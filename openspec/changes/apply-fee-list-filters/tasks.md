## 1. Implementation

- [ ] 1.1 Update fee filter hook APIs as needed to support explicit apply/remove interactions for active chips.
- [ ] 1.2 Replace the fee filter UI with `ListFilters`, preserving filter semantics and responsive behavior.

## 2. Verification

- [ ] 2.1 Add focused coverage for fee filter apply/reset behavior if runtime orchestration changes.
- [ ] 2.2 Run targeted tests and repository checks relevant to the fee filter change.

